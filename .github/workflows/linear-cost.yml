name: Linear Cost Tracker

on:
  pull_request:
    types: [closed]
    branches: [main]
  workflow_dispatch:
    inputs:
      issue_id:
        description: "Linear issue ID (e.g. SH-123)"
        required: true

env:
  HOURLY_COST: "21.35"
  CURRENCY: "£"

jobs:
  update-cost:
    if: >
      github.event_name == 'workflow_dispatch' ||
      (github.event.pull_request.merged == true)
    runs-on: ubuntu-latest
    steps:
      - name: Extract Linear issue ID
        id: extract
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            ISSUE_ID="${{ github.event.inputs.issue_id }}"
          else
            BRANCH="${{ github.event.pull_request.head.ref }}"
            ISSUE_ID=$(echo "$BRANCH" | grep -oE 'SH-[0-9]+' | head -1)
          fi

          if [ -z "$ISSUE_ID" ]; then
            echo "No Linear issue ID found"
            exit 0
          fi

          echo "issue_id=$ISSUE_ID" >> "$GITHUB_OUTPUT"
          echo "Found Linear issue: $ISSUE_ID"

      - name: Get issue title from Linear
        if: steps.extract.outputs.issue_id
        id: linear_issue
        env:
          LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY }}
        run: |
          ISSUE_ID="${{ steps.extract.outputs.issue_id }}"

          ISSUE_DATA=$(curl -s -X POST \
            "https://api.linear.app/graphql" \
            -H "Authorization: $LINEAR_API_KEY" \
            -H "Content-Type: application/json" \
            -d "{\"query\":\"{ issues(filter: { number: { eq: ${ISSUE_ID##*-} }, team: { key: { eq: \\\"${ISSUE_ID%%-*}\\\" } } }) { nodes { id title } } }\"}")

          TITLE=$(echo "$ISSUE_DATA" | jq -r '.data.issues.nodes[0].title // empty')
          UUID=$(echo "$ISSUE_DATA" | jq -r '.data.issues.nodes[0].id // empty')

          if [ -z "$TITLE" ]; then
            echo "Could not find Linear issue $ISSUE_ID"
            exit 1
          fi

          echo "title=$TITLE" >> "$GITHUB_OUTPUT"
          echo "uuid=$UUID" >> "$GITHUB_OUTPUT"
          echo "Found: $TITLE ($UUID)"

      - name: Fetch time from TrackingTime
        if: steps.linear_issue.outputs.title
        id: time
        env:
          TT_AUTH: ${{ secrets.TRACKINGTIME_AUTH }}
          TT_ACCOUNT_ID: ${{ vars.TRACKINGTIME_ACCOUNT_ID }}
        run: |
          TITLE="${{ steps.linear_issue.outputs.title }}"

          # Get all tasks and filter by issue title
          TASKS=$(curl -s \
            "https://app.trackingtime.co/api/v4/$TT_ACCOUNT_ID/tasks" \
            -H "Authorization: Basic $TT_AUTH" \
            -H "User-Agent: LinearCostTracker/1.0")

          # Extract task IDs where name matches the Linear issue title
          TASK_IDS=$(echo "$TASKS" | jq -r --arg title "$TITLE" '.data[]? | select(.name == $title) | .id')

          if [ -z "$TASK_IDS" ]; then
            echo "No TrackingTime tasks found for $ISSUE_ID"
            echo "total_seconds=0" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Fetch time entries for each task and sum durations
          TOTAL_SECONDS=0
          FROM="2020-01-01"
          TO=$(date +%Y-%m-%d)

          for TASK_ID in $TASK_IDS; do
            ENTRIES=$(curl -s \
              "https://app.trackingtime.co/api/v4/$TT_ACCOUNT_ID/events?filter=TASK&id=$TASK_ID&from=$FROM&to=$TO" \
              -H "Authorization: Basic $TT_AUTH" \
              -H "User-Agent: LinearCostTracker/1.0")

            SECONDS=$(echo "$ENTRIES" | jq '[.data[]?.d // 0] | add // 0')
            TOTAL_SECONDS=$((TOTAL_SECONDS + SECONDS))
          done

          echo "total_seconds=$TOTAL_SECONDS" >> "$GITHUB_OUTPUT"
          echo "Total tracked: ${TOTAL_SECONDS}s"

      - name: Calculate cost and post to Linear
        if: steps.linear_issue.outputs.uuid
        env:
          LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY }}
        run: |
          ISSUE_ID="${{ steps.extract.outputs.issue_id }}"
          LINEAR_ISSUE_UUID="${{ steps.linear_issue.outputs.uuid }}"
          TOTAL_SECONDS="${{ steps.time.outputs.total_seconds }}"

          # Calculate hours and cost
          HOURS=$(echo "scale=2; $TOTAL_SECONDS / 3600" | bc)
          COST=$(echo "scale=2; $HOURS * $HOURLY_COST" | bc)

          # Format for display
          if [ "$TOTAL_SECONDS" -eq 0 ]; then
            BODY="**Cost Report** — No time tracked in TrackingTime for this issue."
          else
            BODY=$(printf "**Cost Report**\n- Time tracked: **%sh**\n- Hourly cost: **%s%s**\n- Total cost: **%s%s**" "$HOURS" "$CURRENCY" "$HOURLY_COST" "$CURRENCY" "$COST")
          fi

          echo "Posting to $ISSUE_ID: $BODY"

          # Check for existing cost comment to update
          QUERY=$(jq -n --arg uuid "$LINEAR_ISSUE_UUID" '{query: ("{ issue(id: \"" + $uuid + "\") { comments { nodes { id body } } } }")}')
          COMMENTS=$(curl -s -X POST \
            "https://api.linear.app/graphql" \
            -H "Authorization: $LINEAR_API_KEY" \
            -H "Content-Type: application/json" \
            -d "$QUERY")

          EXISTING_COMMENT_ID=$(echo "$COMMENTS" | jq -r '.data.issue.comments.nodes[] | select(.body | startswith("**Cost Report**")) | .id' | head -1)

          if [ -n "$EXISTING_COMMENT_ID" ]; then
            # Update existing comment
            MUTATION=$(jq -n --arg id "$EXISTING_COMMENT_ID" --arg body "$BODY" \
              '{query: ("mutation { commentUpdate(id: \"" + $id + "\", input: { body: \"" + ($body | gsub("\n"; "\\n")) + "\" }) { success } }")}')
            curl -s -X POST \
              "https://api.linear.app/graphql" \
              -H "Authorization: $LINEAR_API_KEY" \
              -H "Content-Type: application/json" \
              -d "$MUTATION"
            echo "Updated existing cost comment"
          else
            # Create new comment
            MUTATION=$(jq -n --arg uuid "$LINEAR_ISSUE_UUID" --arg body "$BODY" \
              '{query: ("mutation { commentCreate(input: { issueId: \"" + $uuid + "\", body: \"" + ($body | gsub("\n"; "\\n")) + "\" }) { success } }")}')
            curl -s -X POST \
              "https://api.linear.app/graphql" \
              -H "Authorization: $LINEAR_API_KEY" \
              -H "Content-Type: application/json" \
              -d "$MUTATION"
            echo "Created new cost comment"
          fi
